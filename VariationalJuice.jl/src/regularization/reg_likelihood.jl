using StatsFuns


# NOTE (packaging): `soft_loglikelihood` removed here — identical copy kept in src-jl/.

# NOTE (packaging): `init_mar!_with_reg_kernel` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `init_mar!_with_reg_cat_kernel` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `init_mar!_with_reg` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `eval_circuit_with_reg` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `loglikelihoods_probcat` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `loglikelihood_probcat` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).