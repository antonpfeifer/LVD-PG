using ProbabilisticCircuits: tagged_at

##################################################################################
# Downward pass
##################################################################################

# NOTE (packaging): `multi_head_layer_down_kernel` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `multi_head_layer_down` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `multi_head_init_flows_kernel` removed here — identical copy kept in src-jl/ (see src/VariationalJuice.jl).

# NOTE (packaging): `multi_head_init_flows` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `multi_head_flows_circuit` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).

# NOTE (packaging): `multi_head_probs_flows_circuit` removed here — src-jl/ copy wins (see src/VariationalJuice.jl).